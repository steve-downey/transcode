# SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception

name: Continuous Integration Tests

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: '11 9 * * 1'

concurrency:
  group: ${{format('{0}:{1}', github.repository, github.ref)}}
  cancel-in-progress: true

permissions: read-all

jobs:
  preset-test:
    uses: bemanproject/infra-workflows/.github/workflows/reusable-beman-preset-test.yml@1.7.4
    with:
      matrix_config: >
        [
          {"preset": "gcc-debug", "image": "ghcr.io/bemanproject/infra-containers-gcc:latest"},
          {"preset": "gcc-release", "image": "ghcr.io/bemanproject/infra-containers-gcc:latest"},
          {"preset": "llvm-debug", "image": "ghcr.io/bemanproject/infra-containers-clang:latest"},
          {"preset": "llvm-release", "image": "ghcr.io/bemanproject/infra-containers-clang:latest"}
        ]

  build-and-test:
    uses: bemanproject/infra-workflows/.github/workflows/reusable-beman-build-and-test.yml@1.7.4
    with:
      matrix_config: >
        {
          "gcc": [
            { "versions": ["16"],
              "tests": [
                { "cxxversions": ["c++26"],
                  "tests": [
                    { "stdlibs": ["libstdc++"],
                      "tests": [
                        "Debug.Default", "Release.Default", "Release.TSan",
                        "Release.MaxSan", "Debug.Werror",
                        "Debug.Coverage"
                      ]
                    }
                  ]
                },
                { "cxxversions": ["c++23"],
                  "tests": [
                    { "stdlibs": ["libstdc++"],
                      "tests": [
                        "Release.Default"
                      ]
                    }
                  ]
                }
              ]
            },
            { "versions": ["15"],
              "tests": [
                { "cxxversions": ["c++26", "c++23"],
                  "tests": [
                    { "stdlibs": ["libstdc++"],
                      "tests": [
                        "Debug.Default", "Release.Default"
                      ]
                    }
                  ]
                }
              ]
            },
            { "versions": ["14"],
              "tests": [
                { "cxxversions": ["c++26", "c++23"],
                  "tests": [
                    { "stdlibs": ["libstdc++"],
                      "tests": ["Debug.Default", "Release.Default"]
                    }
                  ]
                }
              ]
            },
            { "versions": ["13"],
              "tests": [
                { "cxxversions": ["c++26", "c++23"],
                  "tests": [
                    { "stdlibs": ["libstdc++"],
                      "tests": ["Debug.Default", "Release.Default"]
                    }
                  ]
                }
              ]
            }
          ],
          "clang": [
            { "versions": ["22"],
              "tests": [
                {"cxxversions": ["c++26"],
                  "tests": [
                    { "stdlibs": ["libstdc++"],
                      "tests": [
                        "Debug.Default", "Release.Default", "Release.TSan",
                        "Release.MaxSan", "Debug.Werror"
                      ]
                    }
                  ]
                },
                { "cxxversions": ["c++23"],
                  "tests": [
                    { "stdlibs": ["libstdc++"],
                      "tests": [
                        "Release.Default"
                      ]
                    }
                  ]
                }
              ]
            },
            { "versions": ["21", "20"],
              "tests": [
                { "cxxversions": ["c++26", "c++23"],
                  "tests": [
                    { "stdlibs": ["libstdc++"],
                      "tests": ["Debug.Default", "Release.Default"]
                    }
                  ]
                }
              ]
            },
            { "versions": ["19"],
              "tests": [
                { "cxxversions": ["c++26", "c++23"],
                  "tests": [
                    { "stdlibs": ["libstdc++"],
                      "tests": ["Debug.Default", "Release.Default"]
                    }
                  ]
                }
              ]
            }
          ]
        }

  vcpkg-ci:
    uses: bemanproject/infra-workflows/.github/workflows/reusable-beman-vcpkg-ci.yml@1.7.4
    with:
      port_name: beman-transcode
      feature_combinations: |
        [
          {"features": {}}
        ]

  required-ci:
    name: Required CI
    if: always()
    needs: [preset-test, build-and-test, vcpkg-ci]
    runs-on: ubuntu-latest
    steps:
      - name: Verify required jobs
        env:
          BUILD_AND_TEST_RESULT: ${{ needs.build-and-test.result }}
          PRESET_TEST_RESULT: ${{ needs.preset-test.result }}
          VCPKG_CI_RESULT: ${{ needs.vcpkg-ci.result }}
        shell: bash
        run: |
          for result in \
            "$BUILD_AND_TEST_RESULT" \
            "$PRESET_TEST_RESULT" \
            "$VCPKG_CI_RESULT"; do
            if [[ "$result" != success ]]; then
              echo "A required CI job ended with: $result" >&2
              exit 1
            fi
          done

  create-issue-when-fault:
    needs: [preset-test, build-and-test]
    if: failure() && github.event_name == 'schedule'
    uses: bemanproject/infra-workflows/.github/workflows/reusable-beman-create-issue-when-fault.yml@1.7.4
