# SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
[package]
name = "encoding_rs_bench"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
encoding_rs = "0.8"

[profile.release]
panic = "abort"

[profile.dev]
panic = "abort"
